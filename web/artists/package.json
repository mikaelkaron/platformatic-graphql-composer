{
  "name": "artists",
  "scripts": {
    "test": "borp",
    "start": "plt-db start",
    "clean": "rm -fr ./dist",
    "migrate": "plt-db migrations apply",
    "seed": "plt-db seed seed.ts"
  },
  "devDependencies": {
    "borp": "^0.18.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@platformatic/db": "^2.9.1"
  },
  "engines": {
    "node": "^18.8.0 || >=20.6.0"
  }
}

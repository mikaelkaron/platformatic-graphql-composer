{
  "name": "src",
  "private": true,
  "scripts": {
    "start": "plt-runtime start",
    "clean": "npm run clean --workspaces --if-present --",
    "migrate": "npm run migrate --workspaces --if-present --",
    "seed": "npm run seed --workspaces --if-present --"
  },
  "dependencies": {
    "@platformatic/runtime": "^2.9.1"
  },
  "workspaces": [
    "web/*"
  ]
}
